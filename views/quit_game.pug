extends layout

block content
  h1= title
  hr
  h5 Do you really want to quit from all games?
  h6(class="alert alert-warning", role="alert") All session data like Player Name, etc. will be lost
  div(class="container-fluid text-center px-0")
    div(class="row")
      div(class="col")
        a(class="btn btn-outline-primary w-100", href="/join", role="button") Cancel
      div(class="col")
        // Quit Form
        form(method='POST', action='')
          // Submit Button
          button(type="submit", class="btn btn-danger w-100") Click to quit